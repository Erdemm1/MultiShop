@model List<ResultBrandDto>

<div class="container-fluid py-5">
    <div class="row px-xl-5">
        <div class="col">
            <div class="owl-carousel vendor-carousel">
                @foreach (var item in Model)
                {
                    <div class="bg-light p-4 d-flex justify-content-center align-items-center" style="height:130px; width:220px;">
                        <img src="@item.ImageUrl" alt="" style="max-width:100%; max-height:100%; object-fit:contain;">
                    </div>
                }
            </div>
        </div>
    </div>
</div>
